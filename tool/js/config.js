/* config.js — AI 精批配置
 * 公开仓库不携带任何密钥：apiKey 留空即可，运行时在工具「③诊断室 → AI 设置」里粘贴你的
 * DeepSeek key（保存进浏览器 localStorage），或在本机把 key 直接填到下面（不要提交该改动）。
 */
const IWC_CONFIG = {
  apiKey: "",
  baseUrl: "https://api.deepseek.com",
  model: "deepseek-flash"   // 可选 "deepseek-v4-pro"
};
